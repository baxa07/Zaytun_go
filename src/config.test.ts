import{describe,expect,it}from'vitest'
import{validateEnvironment}from'./config'
const base={VITE_DATA_PROVIDER:'supabase',VITE_SUPABASE_URL:'https://example.supabase.co',VITE_SUPABASE_PUBLISHABLE_KEY:'public-placeholder',VITE_MAP_PROVIDER:'yandex',VITE_YANDEX_MAPS_API_KEY:'maps',VITE_YANDEX_SEARCH_API_KEY:'search',VITE_YANDEX_GEOSUGGEST_API_KEY:'suggest',VITE_DEFAULT_MAP_LAT:'40.1',VITE_DEFAULT_MAP_LNG:'65.3',VITE_DEFAULT_MAP_ZOOM:'14',VITE_PUBLIC_APP_ORIGIN:'https://orders.example.com'}
describe('production environment validation',()=>{it('accepts explicit production-safe providers and formats',()=>expect(validateEnvironment(base,true)).toMatchObject({valid:true,dataProvider:'supabase',mapProvider:'yandex'}));it('rejects local providers and insecure URLs in production',()=>{const result=validateEnvironment({...base,VITE_DATA_PROVIDER:'local',VITE_MAP_PROVIDER:'mock',VITE_PUBLIC_APP_ORIGIN:'http://localhost:5173'},true);expect(result.issues.map(issue=>issue.variable)).toEqual(expect.arrayContaining(['VITE_DATA_PROVIDER','VITE_MAP_PROVIDER','VITE_PUBLIC_APP_ORIGIN']))});it('accepts the legacy public anon variable during migration',()=>expect(validateEnvironment({...base,VITE_SUPABASE_PUBLISHABLE_KEY:undefined,VITE_SUPABASE_ANON_KEY:'public-placeholder'},true).valid).toBe(true))})
